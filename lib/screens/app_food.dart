import 'package:flutter/material.dart';

class FoodItems extends StatelessWidget {
  List food_items;
  FoodItems({Key? key, required this.food_items}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
